import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Players() {

  return (
      <SafeAreaView
        className="h-screen bg-slate-200 dark:bg-zinc-950"
      >
        <Text>player list</Text>
      </SafeAreaView>
    );
}
